# 贡献指南

从 README 的本地开发步骤建立隔离环境，保留工作区已有改动。每个缺陷提交应包含最小复现或失败测试、针对性修复及实际验证结果；不要通过删除测试、扩大授权、吞掉异常或自动重试来制造通过结果。

提交前运行 Ruff 正确性检查、完整模块回归、前端构建和公开发布校验。浏览器测试使用 Chromium 与 WebKit，官方 MCP SDK 兼容测试使用 `.venv-compat`。测试应使用临时项目、回环地址和确定性假进程，不依赖维护者的个人路径、登录态或付费模型。

涉及 Hub/Agent 协议、幂等记录、数据库、文件路径、安装迁移或权限的变更，应额外覆盖失败、断线、重连、取消和恢复路径。数据库迁移必须保留旧数据与可审计恢复结果。旧命名兼容入口是迁移协议的一部分，不应仅因全文搜索命中就删除。

公开提交前检查 `git diff --cached`。不得加入 `.env`、配置、票据、令牌、数据库、`.work`、虚拟环境、浏览器配置、备份或历史部署证据。遵循现有 MIT 许可并保留第三方许可证。提交代码不要求转让版权。

PR 描述应列出影响范围、根因、验证命令、实际结果及未验证的平台。GitHub CI 中未实际运行的步骤不可写为通过；存在跳过、超时或源码变动时必须明确说明。
