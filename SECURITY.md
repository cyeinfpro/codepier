# 安全说明

## 信任边界

CodePier 是可读写文件、执行命令并连接本机开发工具的远程控制软件。只有明确授权的项目与能力可以被访问；安装 Agent 不应等同于授权整个文件系统。浏览器和桌面控制需要独立能力授权及本机所有者选择加入。

Hub 管理授权和持久操作，Agent 在本机再次验证项目路径、写权限和执行策略。SHA 校验与备份降低误覆盖风险，但批量文件操作不是操作系统级原子事务。网络断线后必须查询原操作，不要用新幂等键盲目重试写操作。

## 部署要求

公网使用 HTTPS；仅信任实际反向代理的地址。初始化密码通过交互输入或权限受限的文件提供，不要放在命令参数、仓库、日志或 Issue 中。配对票据、令牌、Agent 配置、数据库和备份都按机密处理。备份恢复及迁移必须在隔离副本上演练，不能以删除旧状态来“修复”安装问题。

非特权容器、只读根文件系统和最小能力有助于缩小影响，但不能代替授权校验。浏览器 HTTP 与 Agent 加密连接具有不同安全边界；Agent 通道加密并不能保护未启用 HTTPS 的面板登录。

## 报告漏洞

仓库创建后优先使用 GitHub 的私密漏洞报告功能（Security → Report a vulnerability）；维护者需要主动启用该功能。在没有可用私密渠道时，先通过仓库维护者公开提供的联系方式取得私密渠道，不要公开发布可复用令牌、私人代码或完整攻击细节。

报告请说明受影响版本、隔离复现步骤、预期与实际结果，以及最小化且已脱敏的证据。不要对不属于你的 Hub、Agent、仓库或第三方服务进行测试。

## 支持与限制

当前发布分支为 1.9.x；旧版用户应先核对迁移与备份步骤再升级。依赖扫描只覆盖已公开、可被扫描器识别的漏洞；静态检查与自动化测试通过不代表不存在未知漏洞。CI 不得使用生产凭据，也不得自动启动付费模型或授予额外桌面权限。
